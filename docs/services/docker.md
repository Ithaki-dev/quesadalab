# Docker documentation

The active Docker platform documentation lives in
[`docker-engine/README.md`](docker-engine/README.md).

This compatibility file is retained because older links may still point to
`docs/services/docker.md`.
