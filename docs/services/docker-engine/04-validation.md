# Validation

Docker service

```bash
systemctl status docker
```

Docker version

```bash
docker version
```

Compose

```bash
docker compose version
```

Hello World

```bash
docker run hello-world
```

Expected Result

Docker should successfully execute the hello-world container.